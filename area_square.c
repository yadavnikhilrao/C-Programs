# include<stdio.h>

// Area of square
int main()
{
    int side;
    printf("Enter the side of area \n");
    scanf("%d", &side);
    printf("Area of the square: %d", side*side);
    return 0;
}